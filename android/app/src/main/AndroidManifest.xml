<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Share card "갤러리 저장" on Android 8–9 only; API 29+ writes via MediaStore without a permission. -->
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:name=".TypeRightApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.TypeRight">

        <!--
            Firebase Analytics 가 기본으로 수집하는 광고 ID(AdvertisingIdClient)를 끈다. 광고를 싣지 않는 앱이라
            필요가 없고, 켜 두면 Play '데이터 보안' 양식에 광고 ID 수집을 신고해야 한다(human-todo B2-1).
            측정하려는 것은 기본 키보드 유지율·리텐션뿐이며 그 지표는 광고 ID 없이도 그대로 나온다.
            google-services.json 이 없으면 계측 자체가 no-op 이라 이 설정도 무의미하다.
        -->
        <meta-data
            android:name="google_analytics_adid_collection_enabled"
            android:value="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Keyboard "로그인하면 AI 훈수" chip → onboarding (Google login step). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="typeright" android:host="login" />
            </intent-filter>
        </activity>

        <!--
            Text-selection menu "[맛춤뻡 검사]" in any app (복사/잘라내기 옆) — works with Samsung Keyboard/Gboard, no
            keyboard switch needed. Started for result by the text view; returns the corrected text when editable.
        -->
        <activity
            android:name=".check.ProcessTextActivity"
            android:excludeFromRecents="true"
            android:exported="true"
            android:label="@string/process_text_label"
            android:theme="@style/Theme.TypeRight.Translucent">
            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <!-- 📸 짤 생성 preview/save/share. Started by explicit class name from the keyboard and the checker. -->
        <activity
            android:name=".share.ShareCardActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:taskAffinity="com.typeright.app.share"
            android:theme="@style/Theme.TypeRight.Translucent" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
</manifest>
