<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.TypeRight" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <!-- RewardAdActivity: transparent window; the Compose dialog draws the popup over the underlying app. -->
    <style name="Theme.TypeRight.Translucent" parent="android:Theme.Translucent.NoTitleBar">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
    </style>
</resources>
