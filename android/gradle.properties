org.gradle.jvmargs=-Xmx4g -Dfile.encoding=UTF-8
org.gradle.caching=true
android.useAndroidX=true
android.nonTransitiveRClass=true
kotlin.code.style=official

# TypeRight backend / Supabase settings (override in ~/.gradle/gradle.properties or with -P).
# typeright.apiBaseUrl.debug=http://10.0.2.2:8790
# typeright.apiBaseUrl.release=https://api.typeright.example
# Empty supabaseUrl => backend dev mode (X-Dev-User-Id header instead of a Supabase bearer token).
# typeright.supabaseUrl=https://<project>.supabase.co
# typeright.supabaseAnonKey=
