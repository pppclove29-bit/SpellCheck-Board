# 사람이 직접 해야 하는 일 (코드로 대체 불가)

> 코드로 가능한 작업은 전부 에이전트가 진행한다. 이 문서는 **계정·결제·심사·실기기·사람 판단**이 필요해서
> 코드로는 끝낼 수 없는 항목만 모은 것이다. 각 항목은 "없으면 무엇이 막히는지"를 함께 적었다.

## 0. 이번 출시 기준 (2026-09-21 결정 — planning-and-dev-log.md 14절)

**AI·결제를 끄고 온디바이스 전용으로 먼저 낸다.** 그래서 이 문서의 대부분이 **이번 출시에서 빠진다.**
표기: **[이번]** 이번 출시에 필요 / **[나중]** AI·결제를 되살릴 때 필요(지금은 손대지 않아도 됨).

이번 출시에 실제로 필요한 것은 이것뿐이다:

| 순서 | 항목 | 상태 |
|---|---|---|
| 1 | A1 Play 개발자 계정 | ✅ **2026-09-21 등록 완료** (세 앱 공용) |
| 2 | A1-1 계정 유형 확인 → 개인이면 클로즈드 테스트(테스터 12명 × 14일) | ⬜ **다음 할 일** |
| 3 | B1 개인정보처리방침 URL | ⬜ 온디바이스 전용이라 내용이 짧아짐 |
| 4 | B2 데이터 보안 양식 | ⬜ "데이터를 수집하지 않음"에 가까워짐 |
| 5 | B3 IME 추가 고지 | ⬜ |
| 6 | C1 매운맛 멘트 48개 검수 | ⬜ 이번 출시의 **주력 기능**이라 더 중요해짐 |
| 7 | C3 아이콘·스크린샷·소개문 | ⬜ 소개문에서 "AI"를 빼야 함 |
| 8 | **A10 릴리스 키스토어 생성 + 비밀번호 보관** | ⬜ **제출 차단 요인.** 코드 쪽은 준비 끝 |
| 9 | A9 Firebase — `google-services.json` 배치 | ⬜ **넣기로 확정** (2026-09-21). 코드는 준비됨, 파일만 넣으면 됨 |
| 10 | D 실기기 확인 | ⬜ D5·D6·D7은 이번 출시 대상이 아님 |

**이번 출시에서 빠지는 것**: A2(구독 상품), A3(OAuth), A4(영수증 검증 서비스 계정), A5·A6(AdMob),
A7(OpenAI 키), A8(Vercel 배포), B4(환불 문구), C2(광고 보상량).
코드는 플래그로 꺼 두었을 뿐 그대로 있으므로, 되살릴 때 이 항목들을 순서대로 하면 된다.

## A. 계정 · 결제 (출시 자체를 막는 것)

| # | 할 일 | 없으면 막히는 것 | 비고 |
|---|---|---|---|
| A1 | **[이번 ✅]** **Google Play Console 개발자 계정 등록 ($25, 1회)** | 앱 출시, 구독 상품 생성, 내부 테스트 트랙 전부 불가 | 본인 확인에 며칠 걸릴 수 있음 → 가장 먼저 |
| A1-1 | **[이번]** **Play Console 계정 유형 확인(개인/조직)** — 개인이면 **테스터 12명 × 14일 연속 클로즈드 테스트**를 통과해야 프로덕션 출시 가능 | 14일이 걸리는 관문이라 이것부터 시작해야 함. 코드는 이미 제출 가능한 상태 | 세 앱이 같은 계정을 쓰므로 한 번만 확인하면 됨 |
| A2 | **[나중]** **Play Console 구독 상품 생성** — `typeright_pro_monthly`(월 2,900원), `typeright_pro_yearly`(연 19,900원) | 결제 화면이 "상품 없음"으로 뜸. 코드는 이 ID로 이미 붙어 있음 | ID는 `SubscriptionProducts`와 **철자까지 동일**해야 함 |
| A3 | **[나중]** **Google Cloud 프로젝트 + OAuth 클라이언트 2개** (Web + Android) | 구글 로그인 불가 → AI 기능·쿼터·PRO 전부 사용 불가 | Android 클라이언트에 **Play 앱 서명 SHA-1** 등록 필수(디버그 SHA-1도 따로 등록해야 개발 중 로그인됨) |
| A4 | **[나중]** **Google Play Developer API 서비스 계정 키 발급** (Cloud Console → Play Console에서 권한 연결) | 구독 영수증 서버 검증 불가 → 결제해도 PRO 권한 안 열림(또는 위조 영수증에 뚫림) | JSON 키는 서버 환경변수로만. 저장소 커밋 금지 |
| A5 | **[나중]** **AdMob 계정 + 앱 등록 + 보상형 광고 단위 생성** | 광고 보상 충전 동작 불가 | 광고 단위 ID·앱 ID를 전달하면 코드에 연결 |
| A6 | **[나중]** **AdMob 콘솔에 SSV 콜백 URL 입력** — `https://<배포도메인>/v1/ads/admob-ssv` | 광고 봐도 서버가 충전 안 함 | 서버 검증 로직은 이미 완성됨 |
| A7 | **[나중]** **OpenAI API 키 발급 + 결제 수단 등록 + 사용량 한도 설정** | AI 문맥 교정 전체 불가(규칙 교정만 동작) | 월 예산 가드가 서버에 있지만 OpenAI 쪽에도 hard limit 걸어둘 것 |
| A8 | **[나중]** **Vercel 계정 + 프로젝트 연결** | 백엔드 배포 불가 | Root Directory `backend`, 리전 `icn1` |
| A9 | **[이번]** **Firebase 프로젝트 생성 → `google-services.json`을 `android/app/`에 배치** | 계측이 전부 무음(no-op)이라 D7/D30·기본 키보드 유지율을 볼 수 없음 | 파일이 없으면 Gradle 플러그인이 아예 적용되지 않게 해 뒀으므로 **빌드는 정상**. 파일만 넣으면 그 순간부터 수집 시작. 패키지명은 `com.typeright.app` |
| A10 | **[이번]** **릴리스 업로드 키스토어 생성 + 비밀번호 보관** — `keytool -genkeypair -v -keystore typeright-upload.jks -alias typeright -keyalg RSA -keysize 2048 -validity 10000` 후 `android/keystore.properties` 작성 | **서명 없는 AAB는 Play에 업로드 불가 = 출시 차단** | 코드 쪽은 끝났다([keystore.properties.example](../android/keystore.properties.example) 참고). **`.jks`와 비밀번호는 저장소에 넣지 않는다** — 파일은 저장소 바깥(예: `~/keys/`)에 두고 별도 백업, 비밀번호는 암호 관리자에. **키스토어를 잃으면 같은 앱을 영영 업데이트할 수 없다** |

## B. 법무 · 심사 서류 (사람이 문장을 확정해야 함)

| # | 할 일 | 없으면 막히는 것 |
|---|---|---|
| B1 | **[이번]** **개인정보처리방침 작성 + 공개 URL 확보** — "키보드 입력 텍스트가 AI 교정을 위해 서버로 전송된다" 조항 필수 | Play 심사 거절. 앱 내 링크가 빈 곳을 가리킴 |
| B2 | **[이번]** **Play Console '데이터 보안' 양식 작성** — 수집 항목(입력 텍스트·이메일), 전송 여부, 암호화, 삭제 요청 경로(앱 내 계정 삭제) | Play 심사 거절 |
| B2-1 | **[이번·A9를 하면]** **데이터 보안 양식에 '앱 활동 분석' 추가** (A9를 하는 경우) | Firebase Analytics를 켰는데 양식에 없으면 정책 위반. 수집하는 것은 이벤트 이름·횟수·enum 값뿐이고 **입력 텍스트는 전송하지 않는다**(`AnalyticsEventTest`가 강제). **광고 ID 수집은 매니페스트에서 껐으므로**(`google_analytics_adid_collection_enabled=false`) 양식에서 광고 ID는 '수집 안 함'으로 답하면 된다 |
| B3 | **[이번]** **IME 앱 추가 고지 확인** — 키보드는 Play에서 입력 데이터 취급을 특히 깐깐하게 봄 | 심사 지연·거절 |
| B4 | **[나중]** **환불/구독 취소 안내 문구 확정** | 결제 관련 심사 지적 사항 |

## C. 사람 판단 · 감수 (기계가 대신 못 하는 것)

| # | 할 일 | 비고 |
|---|---|---|
| C1 | **[이번]** **매운맛 멘트 48개 검수** → [wit-review.md](wit-review.md)의 '수정안' 칸 작성 | "맞춤법만 놀리고 사람은 놀리지 않는다" 기준 위반이 하나라도 있으면 리뷰 테러로 직결 |
| C2 | **[나중]** **광고 보상량 확정** (+3 유지 / +5 상향) | `AD_REWARD_AMOUNT` 환경변수 한 줄. 결정만 내리면 됨 |
| C3 | **[이번]** **앱 아이콘 · 스토어 스크린샷 · 소개문** | 디자인 에셋. 심사 제출물 |
| C4 | **[이번·판단]** **캐릭터 디자인 외주 여부** | 현재 이모지(🌶️/🍎/🚨)로 출시 확정. 반응 보고 재검토 |
| C5 | **[이번]** **중단/피벗 기준 수치 확정** — 예: "D7 리텐션 X% 미만이면 중단" | 계측 코드는 붙일 수 있지만 **기준선은 사람이 정해야 함** |

## D. 실기기 검증 (에뮬레이터로도 일부 가능하나 사람이 눈으로 봐야 함)

**2026-09-21 기준 환경**: 디버그 APK를 맥북 에뮬레이터(AVD `fearless_test`, Pixel 6 / Android 15 / arm64,
`emulator-5554`)에 설치해 둔 상태다. 이 빌드는 **온디바이스 전용**(`typeright.cloudFeatures=false`)이라
로그인·AI·결제·광고가 아예 없다. 온보딩은 1·2단계뿐이고 탭은 3개다.
빌드·설치 경위는 [planning-and-dev-log.md](planning-and-dev-log.md) 13·14절.

판정 표기: **🟢 지금 에뮬레이터로 확인 가능** / **🟡 에뮬레이터로 일부만** (동작 여부는 보이나 최종 판단은 실기기)
/ **🔴 불가** (실기기 또는 A 항목 완료 필요).

'확인 결과' 칸은 비워 두었다. 눌러 본 사람이 `OK` / `문제: <내용>` 형태로 직접 채운다.

| # | 확인할 것 | 판정 | 실기기·계정이 필요한 이유 | 확인 결과 (사람이 채움) |
|---|---|---|---|---|
| D1 | **키보드 타자감** — 키 반응 지연, 조합 중 커서 튐, 오타율 | 🟡 | 조합·커서 버그는 에뮬레이터로도 잡힌다. 다만 마우스 클릭이라 **지연·오타율 체감은 실기기라야 의미 있음** | |
| D2 | **텍스트 선택 메뉴 [맛춤뻡 검사] 노출** — 카톡·인스타·크롬·슬랙 각각 | 🟡 | 에뮬레이터 기본 Chrome·Messages로 노출 자체는 확인 가능. 카톡·인스타·슬랙은 앱마다 `PROCESS_TEXT` 지원이 달라 **해당 앱을 깐 기기**라야 앎 | |
| D3 | **짤 카드 실제 렌더 품질** — 1080×1350 이미지, 폰트 잘림, 말풍선 배치 | 🟢 | 규칙 엔진 훈수에도 📸가 붙으므로 백엔드·로그인 없이 확인 가능 | |
| D4 | **갤러리 저장 · 시스템 공유창** | 🟡 | Android 15 경로는 에뮬레이터로 확인 가능. **8~9 권한 경로**는 해당 API의 AVD나 실기기 필요 | |
| D5 | **[나중]** **구글 로그인 실제 플로우** | 🔴 | **A3 선행**. 지금은 OAuth 클라이언트가 없어 개발 모드 인증으로 대체됨 | |
| D6 | **[나중]** **결제 플로우** — 내부 테스트 트랙 + 라이선스 테스터 계정 | 🔴 | **A1·A2 선행**. Play Billing은 실기기 + 실제 계정 필요 | |
| D7 | **[나중]** **AI 응답 품질 육안 확인** — 되/돼, 반드시/반듯이 등 문맥 오류를 실제로 잡는지, 헛소리 안 하는지 | 🔴 | **A7 선행**(+ 백엔드 기동). 현재 OpenAI는 전부 HTTP 모킹 테스트 | |
| D8 | **저사양/구형 기기 확인** (RAM 3GB, Android 8~10) | 🔴 | 메모리 압박 시 IME 강제 종료는 **실기기라야 재현**됨 | |
| D9 | **이모지 렌더 확인** | 🟡 | 패널 열림·카테고리·최근 사용은 에뮬레이터로 확인 가능. **OEM 폰트(삼성/LG)에서 깨지는 글자**는 실기기라야 앎 | |
| D10 | **상단 행 롱프레스 숫자 타자감** | 🟡 | 입력 자체는 에뮬레이터로 확인 가능. 롱프레스 키는 터치다운이 아니라 **릴리즈에 입력**되므로 느린지는 실기기 체감 — 느리면 되돌릴 수 있게 해 둠 | |
| D11 | **온보딩 1·2단계** — '키보드 설정 열기' → 활성화 → '키보드 선택하기' → TypeRight 선택 | 🟢 | 앱 실행·렌더는 2026-09-21에 확인됨. 실제 활성화 왕복은 사람이 눌러야 함 | |
| D12 | **규칙 교정 동작** — `오늘 몇일이야?`→며칠, `나도 할수있어`→할 수 있, `오늘 진짜 어의가 없네`→어이 | 🟢 | 규칙 JSON이 빌드 때 assets로 번들돼 **완전 온디바이스**. 백엔드·로그인 불필요 | |
| D13 | **피드백 3모드 문구** — 제안 바 모드 칩 🌶️/🍎/🚨 전환 | 🟢 | 규칙 엔진이 모드별 문구를 만든다. 단 **경찰 모드는 무료 플랜이라 진동 경고만**이고 입력 차단(PRO)은 안 되는 게 정상 | |
| D14 | **보안 키패드** — 비밀번호 입력칸에서 제안 바·네트워크가 꺼지는지 | 🟢 | 키보드 앱의 신뢰가 걸린 동작이라 눈으로 한 번은 봐야 함 | |
| D15 | **규칙 오탐 수집** — 평소 쓰는 문장을 쳐 보고 멀쩡한 말을 고치려 드는지 | 🟢 | 오탐이 나오면 `shared/rule-golden-cases.json`의 `clean_cases`에 반례로 추가한다 | |
| D16 | **키 진동 · 키 소리 토글** (설정 탭) | 🟡 | 토글 동작은 보이나 **진동은 에뮬레이터에서 체감 불가**. 소리는 폰 설정의 '터치 사운드'가 켜져 있어야 남 | |

D3·D11~D15(🟢)는 **지금 바로 확인 가능하다.** 여기서 나온 타자감·오탐 피드백이
[planning-and-dev-log.md](planning-and-dev-log.md) 12.6에서 보류한 **클립보드 패널·한글 예측 도입 여부**의 입력값이다.

## E. 순서 권장 (2026-09-21 개정 — 온디바이스 전용 선출시 기준)

1. **A1-1 계정 유형 확인** — 개인 계정이면 테스터 12명 × 14일이 걸린다. **가장 긴 관문이라 오늘 시작**
1-1. **A10 키스토어 생성** — 10분이면 끝나지만 없으면 업로드 자체가 안 된다. A1-1과 같이 해 두면 좋다
2. **D의 🟢 항목(D3·D11~D16)** — 에뮬레이터에 이미 설치돼 있다. 타자감·오탐을 먼저 훑는다
3. **C1 매운맛 멘트 검수** — AI를 뺀 이번 출시에서는 이 멘트가 제품의 핵심 재미다. 우선순위가 올라갔다
4. **B1·B2·B3 (방침·데이터 보안·IME 고지)** — 온디바이스 전용이라 작성할 내용이 크게 줄었다
5. **A9 Firebase (권함)** — 출시 **전에** 켜야 첫 주 리텐션이 남는다. 하면 B2-1도 함께
6. **C3 아이콘·스크린샷·소개문** — 소개문에서 "AI"를 빼야 한다(이번 빌드에 AI가 없다)
7. **C5 중단/피벗 기준** — 이번 출시의 질문은 "기본 키보드로 계속 쓰는가" 하나다
8. **D의 나머지 🟡** — 실기기가 준비되는 대로
9. **(나중) AI·결제 되살리기** — `typeright.cloudFeatures=true` 후
   A3(OAuth) → A7(OpenAI) → A8(Vercel) → A2·A4(구독) → A5·A6(AdMob) → D5·D6·D7 순
